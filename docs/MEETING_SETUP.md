# Meeting setup

- Route the meeting application's output to the cable (or use its virtual
  output, e.g. BlackHole on macOS).
- Choose the **Meeting** preset: longer end-silence (1.1 s), utterances up
  to 40 s, longer context.
- Export the transcript (History → export TXT/SRT/VTT) after the meeting.

# Bundled sidecars

Populated by the build before pnpm tauri build:

- local-squad-sidecar/local-squad-sidecar.exe - PyInstaller-frozen Python sidecar
- 	ranslation-runner.exe - MADLAD-400 candle runner

These binaries are generated, never committed.

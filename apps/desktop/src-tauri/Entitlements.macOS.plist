<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- PERSONAL LOCAL BUILD: intentionally NOT App-Sandboxed.
       The official release profile keeps App Sandbox; this build is for the
       author's own use, where the sandbox breaks local IPC (loopback bind),
       sidecar spawn, and model-store writes with EPERM ("Operation not
       permitted"). No sandbox keys are set: the app reads/writes the real
       home directory like any regular app. -->
  <!-- REQUIRED on macOS 26+: without this entitlement, TCC auto-denies
       microphone requests (silently, no prompt) for ad-hoc-signed apps
       launched via LaunchServices (Finder/double-click). Direct-exec from a
       terminal still worked because access came from the parent chain. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>

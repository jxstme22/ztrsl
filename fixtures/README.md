# Test Fixtures

Phase 0 uses synthetic values only and includes no voice recordings.

Future audio fixtures must be consented or appropriately licensed and must not expose personal
speaker information. Hardware/model-dependent tests must be explicitly marked so CI can skip them
without downloading large models.

